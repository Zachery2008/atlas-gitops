# Org-wide constants only.
# k8s-management/_global.yaml holds Azure tenant-id (global.workload.tenant-id).
# Atlas has no tenant; this path still exists so Argo valueFiles stay valid.
global:
  tags:
    owner: atlas-platform
